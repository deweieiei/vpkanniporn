-- ============================================================
--  Migration: บล็อก "สนใจเป็นตัวแทนประกัน" (รับสมัครตัวแทน)
--
--    recruit_visible = แสดง(1)/ซ่อน(0) บล็อก "สนใจเป็นตัวแทนประกัน" บนหน้าโปรไฟล์
--                      ค่าเริ่มต้น 1 = แสดง (เจ้าของกดปิดได้เองภายหลัง)
--
--  หมายเหตุ: ผู้ที่สนใจสมัครจะถูกเก็บในตาราง contact_inquiries เดิม
--            โดยใช้ kind = 'recruit' (คอลัมน์ kind เป็น VARCHAR อยู่แล้ว ไม่ต้องแก้)
--            รายการจะไปโผล่ในหน้า "รายการผู้ติดต่อ" (/inquiries) ให้เจ้าของเห็น
--
--  วิธีรัน: phpMyAdmin -> เลือก database vpkann_database -> tab SQL -> วาง -> Go
-- ============================================================

ALTER TABLE users
  ADD COLUMN recruit_visible TINYINT(1) NOT NULL DEFAULT 1;
