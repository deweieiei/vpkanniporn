-- ============================================================
--  เพิ่มสถานะ เปิด/ปิด ส่วน "ความสำเร็จและรางวัล" (รายบุคคล)
--    awards_visible = 1  → แสดงบลอคนี้บนหน้าเว็บ (ค่าเริ่มต้น)
--    awards_visible = 0  → ซ่อนทั้งบลอค (ผู้เข้าชมไม่เห็น)
--
--  ▶ วิธีรัน: phpMyAdmin → เลือก database (vpkann_database) → แท็บ SQL → วาง → Go
--    รันครั้งเดียว บน DB ที่มีตาราง users อยู่แล้ว
--    ผู้ใช้เดิมทุกคนจะได้ค่า 1 (แสดง) โดยอัตโนมัติ
-- ============================================================

ALTER TABLE users
  ADD COLUMN awards_visible TINYINT(1) NOT NULL DEFAULT 1 AFTER awards;
