════════════════════════════════════════════════════════════════
  Checkpoint: สิ่งที่ทำเสร็จแล้วในการรื้อรอบนี้
  (ทุกครั้งที่ทำเสร็จอะไร อัปเดตตรงนี้ — ลงชื่อ + วันที่)
════════════════════════════════════════════════════════════════

▌ 2026-06-19 (รอบ 3) — Claude Opus 4.7
  ★ พี่ดิวบอก: "เหมือนเรียก index.html ผิดไฟล์ — แก้และลบที่ไม่ใช้"
    → Launch preview เปิด root/index.html (เก่า) แทน public/index.html (ใหม่)

  ◉ Cleanup:
  ✓ ย้าย root/index.html (23KB, ดีไซน์ reference) → AI_Memory/reference_design_landing.html
     เก็บไว้อ้างอิงสีและโครงสร้าง ไม่ลบทิ้ง

  ✓ ลบ public/packages.html (placeholder ไม่ตรง requirement)
     — ลูกค้าต้องการ /plans dynamic (Phase 3) ไม่ใช่ static placeholder

  ✓ ลบ route /packages ใน routes/pages.js

  ✓ ลบ link /packages ใน public/nav.js (เหลือ หน้าหลัก + ค้นหาตัวแทน)

  ✓ แก้ public/index.html service cards 6 ใบ:
     link "/packages?category=..." → "#contact" (พา visitor ไป CTA ติดต่อ)

  ◉ UI improvements (สืบเนื่องจาก feedback "ไม่เห็น login/register"):
  ✓ public/index.html ปุ่ม header
     เก่า: วงกลม "→" icon เล็กๆ (เห็นยาก)
     ใหม่: ปุ่มข้อความ "✍️ สมัครสมาชิก" + "🔑 เข้าสู่ระบบ"
     หลัง login: "⚙ หน้าจัดการ" + "↩ ออกจากระบบ"
     Mobile (<640px): ซ่อนปุ่มสมัครสมาชิก เหลือแค่เข้าระบบ

  ✓ public/login.html: รับ window.location.hash === '#register'
     → เปิด tab สมัครสมาชิกอัตโนมัติเวลามาจาก landing

  ◉ Smoke test ผ่าน:
     GET /         → HTTP 200 (landing ใหม่)
     GET /packages → HTTP 404 (ลบสำเร็จ)
     GET /nav.js   → ไม่มี packages link
     GET /dashboard → 302 → /login (auth gate)

▌ 2026-06-19 (รอบ 2) — Claude Opus 4.7
  ★ พี่ดิวยืนยัน: ทดสอบด้วย Docker MySQL local, nav 6 อันอยู่ใน dashboard ตัวแทนเท่านั้น,
    "ผลงาน" = รางวัล/Timeline ความสำเร็จ

  ◉ ค้นพบที่ต้องแก้:
  ★ index.js (Express entry) หายไปจาก disk + ไม่อยู่ใน git → เขียนใหม่
  ★ เครื่องพี่ดิวไม่มี MySQL/XAMPP → ใช้ Docker

  ◉ Code:
  ✓ index.js — Express server เต็มรูปแบบ
     - express-session cookie 'fwd.sid' 30 วัน
     - mount /api/auth, /api/users, /
     - /health, /api/health/db
     - SPA fallback 404 → index.html
     - graceful shutdown (SIGINT/SIGTERM ปิด MySQL pool)
     - smoke test ผ่าน: /, /login, /search, /api/auth/me ทำงานปกติ

  ✓ docker-compose.yml — MySQL 8.0 + phpMyAdmin
     - port 3306 (MySQL) + 8080 (phpmyadmin GUI)
     - utf8mb4_unicode_ci, TZ=Asia/Bangkok
     - mount db/schema.sql ใน /docker-entrypoint-initdb.d/ → auto-init
     - volume fwd-mysql-data (persistent)
     - DB user: vpkann_database / pass: devpass (local)

  ✓ .env — เปลี่ยน DB_PASSWORD เป็น 'devpass' (local Docker)
     - production .env บน HostAtom ต้องใช้ pass จริง

  ✓ public/index.html — landing nav แบบ visitor-friendly
     - nav header เหลือ 2 อัน: หน้าแรก / ค้นหาตัวแทน
     - ปุ่มมุมขวาบน: ลูกศร → /login (default)
     - JS เช็ค /api/auth/me ตอน load: ถ้า login → ปุ่มเปลี่ยนเป็น ⚙ /dashboard

  ✓ public/dashboard.html — หน้าจัดการสำหรับตัวแทน (ใหม่)
     - Top header: logo + ชื่อตัวแทน + avatar + logout
     - Banner "คุณกำลังอยู่ในโหมดจัดการข้อมูล"
     - Sidebar 6 อัน:
       1. 💼 วางแผนการเงิน — placeholder
       2. 🏥 ประกันสุขภาพ — placeholder (รอ DB Phase 3)
       3. 💰 ออมเงินและมรดก — placeholder (รอ DB Phase 3)
       4. 🏆 ผลงานและรีวิว — ★ ใช้งานได้จริง ★
            - textarea 1 บรรทัด = 1 รางวัล
            - live preview เป็น tag pills
            - บันทึก → PUT /api/auth/profile (awards JSON)
       5. 🤝 ร่วมงานกับเรา — placeholder
       6. 📞 ติดต่อเรา — ★ ใช้งานได้จริง ★
            - แก้ phone / line_id / facebook_url / instagram_url
            - email read-only (เปลี่ยนที่หน้าโปรไฟล์)
     - Tab switching ด้วย URL hash
     - Auth check ตอน load: ถ้าไม่ login → redirect /login

  ✓ routes/pages.js — เพิ่ม route /dashboard (requireAuth → redirect /login)

▌ พี่ดิวต้องทำ (1 step):
  ◇ ลง Docker Desktop https://www.docker.com/products/docker-desktop/
    หลังลงเสร็จเปิด Docker Desktop แล้วบอก AI ต่อรอบหน้า
    AI จะรัน docker compose up -d ให้ → ทดสอบ register + login ได้จริง

▌ 2026-06-19 (รอบ 1) — Claude Opus 4.7
  ✓ สำรวจ codebase + DB schema เรียบร้อย
  ✓ อ่าน PDF + 4 รูปภาพในโฟลเดอร์ "สิ่งที่ต้องทำ/"
  ✓ ตีความเครื่องหมายแดงของลูกค้าได้ครบ 7 หน้า PDF
  ✓ สร้าง AI_Memory/ + เขียนเอกสารส่งต่อ 9 ไฟล์
  ✓ พี่ดิวยืนยัน: Step-by-step UI ก่อน, กวิสรา showcase, public landing first

  ◉ Code changes:
  ✓ public/index.html — รื้อใหม่เป็น marketing landing (Phase 1.1)
     - ดีไซน์ตาม S__13730279.jpg เป๊ะ: hero/services 6 ใบ/why-us 4 stats/
       profile + popular plans/3 testimonials/timeline 2022-25/career/CTA
     - สีจาก CSS variables FWD (--primary-orange #FF6B00)
     - มี nav.js placeholder + sticky LINE CTA ขวาล่าง
     - ปุ่ม login icon มุมขวาบน (→ /login)
     - ปุ่ม "ดูโปรไฟล์ฉบับเต็ม" → /agent/1
     - JS fetch /api/users/1 มา override ข้อมูลกวิสรา (มี fallback static)
     - Responsive: clamp() ทุก font + breakpoint 640px/1024px
     - Service cards ลิงก์ไป /packages?category=health|life|savings|...

  ✓ public/profile.html — ลบ awards + bio sections + เพิ่ม view-as-prospect (Phase 1.2)
     - ลบ section "แนะนำตัว" (bio block) — ตาม PDF หน้า 2 ที่ลูกค้าวง X
     - ลบ section "เกียรติประวัติและผลงาน" — ตาม PDF หน้า 2 ที่ลูกค้าวง X
     - เพิ่มปุ่ม "👁️ ดูในมุมมองผู้มุ่งหวัง" ข้างปุ่ม "แก้ไขโปรไฟล์"
       (โชว์เฉพาะตอนดูโปรไฟล์ตัวเอง, คลิก → /agent/{own-id})
     - เก็บ edit modal เดิม (bio + awards ยังบันทึก DB ได้ ใช้ในอนาคต)
     - เก็บ hero/CTA row/profile card/license box เดิม (ตรงตาม PDF)
     - ลบโค้ด JS render bio + awards (element ถูกลบ)

▌ ยังไม่ทำ:
  - Admin dashboard (S__13730278.jpg) — ขั้นต่อไป
  - Plans management (PDF หน้า 4-6)
  - Edit profile ใหม่ (PDF หน้า 7) — เพิ่ม Threads, Line OA, ลบสาขา
  - DB tables ใหม่ (insurance_plans, contact_submissions)
  - ทดสอบในเบราว์เซอร์จริง (พี่ดิวเปิดดู preview ได้)

════════════════════════════════════════════════════════════════
  (เพิ่มรายการใหม่ด้านบน — ลำดับใหม่สุดอยู่บนสุด)
════════════════════════════════════════════════════════════════
