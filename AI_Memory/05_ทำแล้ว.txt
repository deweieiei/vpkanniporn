════════════════════════════════════════════════════════════════
  Checkpoint: สิ่งที่ทำเสร็จแล้วในการรื้อรอบนี้
  (ทุกครั้งที่ทำเสร็จอะไร อัปเดตตรงนี้ — ลงชื่อ + วันที่)
════════════════════════════════════════════════════════════════

▌ 2026-07-19 (รอบ 17 — เก็บงานย่อย 3 อย่างตามที่พี่ดิวสั่ง) — Claude Opus 4.8

  ✓ hero: เอาป้าย 4 อันออก (❤️ ประกันชีวิต / ➕ สุขภาพ / 🐷 การออม / 🎁 มรดก)
     - public/home.html renderHero() — ตัดตัวแปร tags + <div> ที่แสดงป้ายทิ้ง
     - data.tags ยังค้างใน DB (ไม่ได้ลบ ไม่มีพิษภัย) แค่ไม่ได้แสดงแล้ว
     - ★ เมนูบนหัวเว็บ "ประกันสุขภาพ / ออมเงินและมรดก" เป็นคนละอัน — ไม่ได้แตะ

  ✓ hero: เอารูปที่ปรึกษาด้านขวาออก (พี่ดิวสั่งเพิ่มทีหลังในรอบเดียวกัน)
     - ตัด <div class="relative flex justify-center md:justify-end"> ทั้งก้อน
       (ข้างในมี /src/S__12566769.jpg — เป็นที่เดียวที่ใช้รูปนี้ + คลาส feat-photo)
     - ★ ต้องแก้กริดแม่ด้วย: เดิม grid md:grid-cols-2 พอเหลือลูกเดียว
       ข้อความจะถูกบีบอยู่ครึ่งซ้าย เหลือที่ว่างครึ่งขวา
       → เอา grid ออก + ใส่ max-w-3xl ที่กล่องข้อความกันบรรทัดยาวเกิน

  ✓ plans: แก้ปุ่มได้ "ที่ปุ่มเลย" ไม่ต้องกดเข้าไปข้างใน
     - public/home.html renderPlans() — แอดมินแก้ชื่อปุ่ม + ใส่/เปลี่ยน/ลบรูป ได้บนหน้าแรก
     - ★ แอดมินได้การ์ดเป็น <div> + ลิงก์ "เปิดดู" แยก / คนทั่วไปได้ <a> ครอบทั้งใบเหมือนเดิม
       เหตุผล: ถ้าเอา contenteditable ไปแปะใน <a> พอคลิกจะเด้งออกจากหน้าทันที พิมพ์ไม่ได้
     - ตั้งใจ "ไม่" ใส่ปุ่มลบบนหน้าแรก — ลบปุ่มนี้ = หน้าย่อย+โพสหายทั้งสาย เสี่ยงกดพลาด
       ให้ไปกด "🗑 ลบบลอคนี้" ในหน้าข้างในเหมือนเดิม

  ✓ blockpage: กล่องเขียนโพสเหลือเฉพาะ "หน้าสุดท้าย"
     - editor แสดงเมื่อ (IS_ADMIN && !canAddKid()) — ชั้นบนเป็นหน้ารวมปุ่มอย่างเดียว
     - contentView ยังแสดงทุกชั้น → ถ้าชั้นบนเคยมีเนื้อหาเขียนไว้ คนอ่านยังเห็น ไม่หายเงียบ
       (แต่แอดมินจะแก้เนื้อหานั้นจากหน้านั้นไม่ได้แล้ว — ยังไม่เจอเคสนี้บน production)

  ✓ การ์ด (features/awards): ใส่ "รูป" แทนการพิมพ์อีโมจิได้แล้ว
     - public/home.html cardHtml() — วงกลมหัวการ์ดอ่านรูปจาก c.images (slot 0, variant web)
       มีรูป → โชว์รูป + ปุ่ม × ลบรูป · ไม่มีรูป → ถอยไปใช้อีโมจิเดิม (แก้ได้เหมือนเดิม)
     - ปุ่ม "🖼️ ใส่รูปแทนไอคอน" / "🖼️ เปลี่ยนรูป" ใต้วงกลม (เห็นเฉพาะแอดมิน)
     - ★ ไม่ต้องแก้ API เลย — handler [data-up]/[data-del] เดิมอ่าน id+slot+variant
       จาก dataset อยู่แล้ว ใส่ attribute ให้ถูกก็ใช้ /api/blocks/:id/image ได้ทันที
     - ★ ไม่ตัดอีโมจิทิ้ง เพราะการ์ดเก่าบนเว็บจริงมีแต่อีโมจิ ถ้าตัดจะกลายเป็นวงกลมว่าง
     - ⚠️ cardHtml ใช้ร่วมกันทั้ง features และ awards → "ความสำเร็จและรางวัล" ได้ตามไปด้วย

  ✓ จำกัดความลึกบลอค: สร้างบลอคย่อยได้อีกชั้นเดียว แล้วเป็นหน้าเขียนโพส
     - public/blockpage.html — เก็บ breadcrumb ไว้ในตัวแปร CRUMB
       canAddKid() = CRUMB.length <= 1
       ชั้น 1 (ลูกของ plans) breadcrumb=[plans]        → มีปุ่ม ＋ สร้างบลอคย่อย
       ชั้น 2 (ลูกของชั้น 1)  breadcrumb=[plans,ชั้น1] → ไม่มีปุ่มแล้ว = หน้าเขียนโพส
     - แถบแอดมินด้านบนเปลี่ยนข้อความตามชั้น ("หน้าเนื้อหาบลอค" / "หน้าเขียนโพส")
     - แก้เงื่อนไขตารางปุ่มเป็น (CHILDREN.length || addKid) ไม่งั้นชั้นลึกจะเหลือกริดว่าง
     - กล่องเขียนโพสยังมีทุกชั้นเหมือนเดิม (ชั้นลึกแค่เปลี่ยนหัวเป็น "📝 เขียนโพส")
     - ⚠️ กันแค่ฝั่งหน้าเว็บ — API POST /api/blocks ยังสร้างลึกกี่ชั้นก็ได้

  ✓ โมดัลเลือกตัวแทน: เพิ่มช่องค้นหา ไม่แสดงรายชื่อทั้งหมดแล้ว
     - public/home.html — เพิ่ม <input id="pick-search"> ค้นได้ทั้งชื่อ/นามสกุล/ตำแหน่ง
     - เปิดโมดัลมา → โชว์เฉพาะคนที่เลือกไว้ ยังไม่พิมพ์ก็ยังไม่โชว์ใคร
     - ★ ที่ติ๊กไว้เก็บใน Set ชื่อ pickSel ไม่ได้อ่านจาก DOM ตอนกดบันทึก
       (ถ้าอ่านจาก DOM แบบเดิม พอกรองรายชื่อ คนที่ติ๊กแต่ถูกกรองออกจะหลุดหายไป)
     - คนที่เลือกไว้แต่ไม่ตรงคำค้น ยังโชว์ค้างไว้ท้ายรายการ ให้เห็นว่าไม่ได้หลุด

  ✓ เทสต์แล้วด้วย jsdom (ไม่ต้องมี DB — ปลอม fetch เอา) ผ่าน 25 ข้อ
     ครอบคลุม: ป้าย hero หายจริง + ข้อความอื่นไม่หายเกิน / ปุ่มสร้างบลอคชั้น 1-2-3 /
     ค้นหาตัวแทน + ติ๊กแล้วเปลี่ยนคำค้นไม่หลุด + กดบันทึกส่ง agent_ids ถูกต้อง
     ★ ระวังตอนเขียนเทสต์: body.textContent รวมโค้ดใน <script> ด้วย
       → ตอนแรกเทสต์ตกเพราะไปเจอชื่อป้ายในคอมเมนต์ ไม่ใช่โค้ดพัง

▌ 2026-07-16 (รอบ 16 — รื้อหน้าตาเว็บเป็น "ระบบบลอค") — Claude Opus 4.8
  ★ รายละเอียดเต็มอยู่ใน HANDOFF_2026-07-16.txt — อ่านอันนั้นเป็นหลัก
  ★ พี่ดิวสั่ง: "ลื้อระบบเลย ดูว่าแดชบอร์ดต้องการอะไร ทำ sql ใหม่เลย ภาพบลอค"
    แล้วไล่สเปคมา 8 ข้อ (Hero 18 รูป / เลือกแผนซ้อนบลอค / ทำไมต้องวางแผน /
    รู้จักที่ปรึกษา 3-5 คน / ความสำเร็จ / เอา reviews ออก / กำลังมองหาอาชีพ / CTA)
    และเคาะว่า "พิมพ์ vpkanniporn.com มาแล้วต้องเป็นหน้านี้ มันคือหน้าตาเว็บ"

  ◉ ทำเสร็จ + ขึ้น production แล้ว:
  ✓ ตาราง page_blocks + block_images (db/insert_blocks_only.sql = ตัวที่รันสำเร็จจริง)
  ✓ routes/blocks.js — API ระบบบลอค (สร้าง/แก้/ลบ/สลับลำดับ/ซ่อน/อัปรูป 18 ช่อง)
  ✓ public/home.html — รื้อใหม่ render จากบลอค + แอดมินคลิกแก้บนหน้าจริงได้เลย
  ✓ public/blockpage.html + route /b/:id — หน้าเนื้อหาบลอค ซ้อนชั้นไม่จำกัด + เขียนโพส
  ✓ / เสิร์ฟ home.html แล้ว (เดิมเสิร์ฟ dashboard.html) — ปิดคำถามที่ค้างมาตั้งแต่ 6 ก.ค.
  ✓ เอาระบบ "หน้า Home เลือกบัญชี + แก้ไขเนื้อหา" + impersonate ออกหมด (พี่ดิวสั่ง)
  ✓ ลง MySQL 8.4 + MariaDB 12.3 ในเครื่อง dev → ปัญหา "ไม่มี DB เทสต์" ที่ค้างมา
    ตั้งแต่รอบ 14 จบแล้ว
  ✓ พี่ดิวเห็นของจริงแล้ว: "มันสุดยอด"

  ◉ บั๊กที่เจอ + แก้แล้ว (ห้ามลืม — จดไว้ใน HANDOFF ข้อ 1-5 ด้วย):
  ✗→✓ production เป็น MariaDB ไม่ใช่ MySQL → CAST(? AS JSON) พัง แก้เป็น string ธรรมดา
  ✗→✓ CREATE TABLE IF NOT EXISTS ข้ามการแก้ schema เก่า → ERROR 1048 cannot be null
  ✗→✓ HostAtom อ่าน information_schema ไม่ได้ (#1044) → ห้ามใส่ในไฟล์ที่ส่งให้พี่ดิวรัน
  ✗→✓ GET /api/blocks/:id ไม่ส่งรูปของบลอคลูก → รูปบนปุ่มย่อยไม่ขึ้น

▌ 2026-07-06 (รอบ 15 — login เป็นหน้าแรก + ระบบปุ่ม/หน้าของ user) — Claude Opus 4.8
  ★ พี่ดิวสั่ง: (1) / = หน้า login, ย้ายหน้าค้นหาเดิมไป /home
    (2) login ไม่จำ (ปิดเบราว์เซอร์ต้องล็อกอินใหม่) ทั้ง admin/user
    (3) แต่ละ user เพิ่ม "ปุ่ม/หน้า" เองได้ไม่จำกัด (text+รูป) กดแล้วเปิดหน้าเนื้อหา
        คนทั่วไปเห็นเนื้อหา / เจ้าของที่ล็อกอินเห็น toolbar แก้ไข — ขอ SQL ไปรันเอง

  ◉ ทำเสร็จ:
  ✓ db/migration_user_pages.sql (ไฟล์ใหม่) — ตาราง user_pages
    (user_id, button_text, button_image, content, sort_order, is_active) 1 แถว=1 ปุ่ม=1 หน้า
  ✓ routes/userpages.js (ใหม่) mount /api:
    GET /api/pages?user_id=N (public list ปุ่ม), GET /api/pages/mine (เจ้าของ),
    GET /api/pages/:id (public เนื้อหา + is_owner), POST /api/pages (สร้าง),
    PUT /api/pages/:id, POST /api/pages/:id/image, DELETE /api/pages/:id (เช็คเจ้าของทุกตัว)
  ✓ public/page.html (ใหม่) + route /page/:id — แสดงเนื้อหาหน้า; เจ้าของเห็น toolbar แก้ (title/รูป/เนื้อหา)
  ✓ index.js session: rolling=false + ไม่ตั้ง cookie.maxAge = session cookie (ไม่จำ login)
  ✓ routing: / = login.html (ล็อกอินแล้ว→/home), /home = หน้าค้นหา (เดิม index.html → เปลี่ยนชื่อเป็น home.html)
    login สำเร็จ → ไป /home
  ✓ ทดสอบ (ไม่มี DB): syntax ผ่าน, / = login, /home = ค้นหา, /page/:id = 200,
    /api/pages ทำงาน (500/401 = mount แล้ว)

  ◉ ยังไม่ทำ (รอ DB + งานหน้าโปรไฟล์):
  - ผูกปุ่ม user_pages เข้าไปแสดงใน "เลือกแผนที่เหมาะกับคุณ" บนหน้าโปรไฟล์ (dashboard.html)
    + ปุ่ม "เพิ่มหน้า" สำหรับเจ้าของ — รวมกับงานแต่งหน้าโปรไฟล์ (ทำตอนมี DB จะเห็นผลจริง)

▌ 2026-07-06 (รอบ 14 — เปลี่ยนทิศเว็บ: Index=ค้นหาตัวแทน + Admin จัดการตัวแทน) — Claude Opus 4.8
  ★ พี่ดิวเปลี่ยนทิศทางเว็บครั้งใหญ่:
    (1) Index = "ค้นหาตัวแทนอย่างเดียว" เอาส่วนการตลาด (hero/บริการ/รีวิว/รางวัล/career) ออกหมด
    (2) Admin = "จัดการตัวแทน" (เพิ่ม/ปิด-เปิด/ลบ ผู้ใช้) — เลิกระบบแก้ข้อความ CMS ไปเลย
    (3) ตัดหน้าลงทะเบียน (สมัครเอง) ออก — ให้ admin สร้างบัญชีให้แทน
    (4) [ยังไม่ทำ] แต่งหน้าโปรไฟล์ (dashboard.html) ให้สวยเหมือน index — ยกไปรอบหน้า (ไฟล์ 1583 บรรทัด)

  ◉ Admin login (พี่ดิวรัน node script เองไม่ได้):
  ✓ db/seed_admin.sql (ไฟล์ใหม่) — รันใน phpMyAdmin ไฟล์เดียวได้บัญชี admin/1234 (bcrypt)
    มีคำสั่ง UPDATE (comment ไว้) สำหรับรีเซ็ตรหัสถ้าล็อกอินไม่ได้ + คำสั่งนี้อยู่ใน migration ด้วย

  ◉ จัดการตัวแทน (แทน CMS):
  ✓ routes/admin.js (ไฟล์ใหม่) mount /api/admin — GET/POST/PUT/DELETE /users (เฉพาะ role admin)
    - POST เพิ่มผู้ใช้ (email+password bcrypt, role agent/user/admin), PUT แก้/เปลี่ยนรหัส/สถานะ,
      DELETE ลบ — กันปิด/ลบ/ลดสิทธิ์บัญชีตัวเอง
  ✓ public/supperadmin-users.html (ไฟล์ใหม่) — ฟอร์มเพิ่มผู้ใช้ + ตารางรายชื่อ (ปิด/เปิด/ลบ)
  ✓ routes/pages.js — /support-admin/users (หน้าจัดการ), /support-admin/editor → redirect ไป /users
  ✓ supperadmin-login.html — หลัง login ไป /support-admin/users

  ◉ เลิกระบบ CMS แก้ข้อความ index:
  ✓ ลบ routes/content.js + public/supperadmin-editor.html + ถอด mount ใน index.js
    (ตาราง site_content ยังอยู่ใน DB แต่ไม่ได้ใช้แล้ว — ไม่ลบ ไม่กระทบ)

  ◉ Index ใหม่ = หน้าค้นหาตัวแทน:
  ✓ เขียน public/index.html ใหม่ทั้งหน้า (ทำจากฐาน search.html) — โลโก้ + แถบส้ม + ช่องค้นหา
    (ชื่อ/รหัส/จังหวัด/สาขา) + การ์ดตัวแทน (ดึง /api/users) + ปุ่มเข้าสู่ระบบ — เอา data-ck/CMS ออกหมด

  ◉ ตัดหน้าสมัคร:
  ✓ พี่ดิวจัดการเองแล้ว: ลบ route /register ใน auth.js + ลบฟอร์มสมัครใน login.html
    (login.html เหลือแค่ล็อกอิน + โลโก้ + พื้นหลัง — ตรวจแล้วสมบูรณ์)

  ◉ ทดสอบ (ไม่มี DB): node --check ผ่านหมด, server รันสะอาด, index/support-admin = 200,
    /api/admin/users = 403 (ต้องล็อกอิน), /api/content = 404 (CMS หายแล้ว), ไม่มี JS error

▌ 2026-07-06 (รอบ 13 — Support Admin: ระบบแก้เนื้อหาหน้าแรก) — Claude Opus 4.8
  ★ พี่ดิว: "ต้องการเพิ่ม Support Admin ที่แก้ข้อความ+ภาพที่ index ได้
    ช่วยเพิ่มในส่วนของดาต้าเบส หน้า login ของ Support Admin"
  ★ เคาะกับพี่ดิวก่อนทำ: (1) สร้างบัญชี Support Admin 1 บัญชี (ใช้ระบบ login
    เดิมแต่แยกหน้า) (2) แก้ได้ทั้งข้อความและรูปทั้งหมดบนหน้า index

  ★★ แก้เพิ่มตามที่พี่ดิวสั่ง (รอบเดียวกัน บ่าย 6 ก.ค.):
     - เปลี่ยนชื่อเรียกเป็น "SupperAdmin" (สะกดตามที่พี่ดิวต้องการ)
     - ใช้ role 'admin' (ไม่ใช่ 'support_admin') — เช็คสิทธิ์ทุกที่เป็น role==='admin'
     - บัญชี: user 'admin' / password '1234' (bcrypt hash ใส่ใน migration แล้ว)
       ล็อกอินด้วย "ชื่อผู้ใช้" ไม่ใช่อีเมล — เก็บ 'admin' ในคอลัมน์ email
     - API แยก: POST /api/auth/supperadmin-login ({username,password})
     - สคริปต์เปลี่ยนเป็น scripts/create_supperadmin.js (ลบ create_support_admin.js)
     - หน้า login: พื้นหลังเต็มจอจาก public/assets/background.svg + โลโก้ Logo.svg
       (พี่ดิวเอาไฟล์ 2 ตัวใส่ใน public/assets/ เอง) + เปลี่ยนช่องเป็น "ชื่อผู้ใช้"
     - หมายเหตุ: enum ยังมีค่า 'support_admin' ค้างอยู่ (ไม่ได้ใช้แล้ว ไม่ลบ เพราะไม่กระทบ)

  ★★★ แก้เพิ่มอีก (พี่ดิวสั่งต่อ 6 ก.ค.): เปลี่ยนชื่อไฟล์ + แต่งหน้า login ผู้ใช้ทั่วไป
     - เปลี่ยนชื่อไฟล์ (git mv) ให้ตรงกับชื่อ SupperAdmin:
         public/support-login.html   → public/supperadmin-login.html
         public/support-editor.html  → public/supperadmin-editor.html
         db/migration_support_admin.sql → db/migration_supperadmin.sql
       (อัปเดต path ใน routes/pages.js + README แล้ว; URL ยังเป็น /support-admin เหมือนเดิม)
     - public/login.html (หน้า login/สมัคร ของตัวแทน/ผู้ใช้ทั่วไป):
       เปลี่ยนโลโก้ตัวอักษร "FWD 🛡️" → รูปจริง /assets/Logo.svg + พื้นหลัง /assets/background.svg
       ให้เข้าชุดกับหน้า SupperAdmin (ฟอร์ม login/register เหมือนเดิม ไม่แตะ logic)

  ◉ Database:
  ✓ db/migration_support_admin.sql (ไฟล์ใหม่ — ยังไม่ได้รันบน DB จริง!)
     - ALTER users.role เพิ่มค่า 'support_admin' (ของเดิม admin/agent/user)
     - CREATE TABLE site_content (content_key PK, section, label, content_type
       text|image, content_value, sort_order) — เก็บเนื้อหาหน้า index เป็น key→ค่า
     - INSERT IGNORE ค่าตั้งต้น ~70 key = ข้อความ/รูปบนหน้า index ปัจจุบัน
       (จัดกลุ่ม section + label ไทย ไว้ให้หน้า editor สร้างฟอร์มอัตโนมัติ)
  ✓ scripts/create_support_admin.js (ไฟล์ใหม่) — สร้าง/รีเซ็ตบัญชี support admin
     วิธีใช้: node scripts/create_support_admin.js <email> <password> [ชื่อ] [นามสกุล]
     (bcrypt hash, upsert, ตั้ง role=support_admin) — ยังไม่ได้รัน (รอ DB)

  ◉ Backend:
  ✓ routes/content.js (ไฟล์ใหม่) — mount ที่ /api ใน index.js
     - GET  /api/content        (public) คืน map {key:value} ให้หน้า index
     - GET  /api/content/admin  (support admin) คืนข้อมูลเต็มไว้สร้าง editor
     - PUT  /api/content        (support admin) บันทึกข้อความหลายช่อง (transaction,
       อัปเดตเฉพาะ key ที่มีจริง + ชนิด text เท่านั้น)
     - POST /api/content/image  (support admin) อัปโหลดรูป → /uploads/site/,
       ลบรูปเก่าอัตโนมัติ, กัน path traversal
  ✓ routes/auth.js — login/register เก็บ req.session.role เพิ่ม (จำเป็นต่อการเช็คสิทธิ์)
     login SELECT ดึง role มาด้วย + คืน role ใน response
  ✓ routes/pages.js — เพิ่ม GET /support-admin (หน้า login) + /support-admin/editor
     (มี requireSupportAdmin: ถ้าไม่ใช่ → redirect กลับ /support-admin)

  ◉ Frontend:
  ✓ public/support-login.html (ไฟล์ใหม่) — หน้า login แยกของ Support Admin
     (โทนส้ม FWD, เช็ค role==='support_admin' ถ้าไม่ใช่ logout+แจ้งเตือน)
  ✓ public/support-editor.html (ไฟล์ใหม่) — หน้าจัดการเนื้อหา
     - ดึง /api/content/admin มาสร้างฟอร์มอัตโนมัติ จัดกลุ่มตาม section
     - ข้อความ = textarea auto-resize, กด "บันทึกทั้งหมด" → PUT
     - รูป = เลือกไฟล์แล้วอัปโหลดทันที (POST) + แสดง preview
     - จัดการ session หมดอายุ (401/403 → เด้งกลับ login)
  ✓ public/index.html — ใส่ data-ck="key" 74 จุด + data-ck-bg (career) 1 จุด
     - เพิ่ม script loadSiteContent(): fetch /api/content มาแทนข้อความ/รูป
       ถ้าดึงไม่ได้ ใช้ข้อความ static เดิมเป็น fallback (ไม่พัง)
     - แก้บั๊กเดิม: script ท้ายหน้าอ้าง element cta-login/cta-logout ที่ไม่มีจริง
       → throw ตั้งแต่ต้น ทำให้ loadFeaturedAgent ไม่เคยทำงาน — ลบโค้ดตายทิ้งแล้ว

  ◉ ทดสอบแล้ว (เท่าที่ทำได้โดยไม่มี DB):
  ✓ node --check ผ่านทุกไฟล์, server restart สะอาด, mount router ได้
  ✓ หน้า index render ครบ (fallback static) ไม่มี JS error, / = 200
  ✓ /support-admin = 200 (มีฟอร์ม), /support-admin/editor เด้ง login เมื่อไม่ล็อกอิน
  ✓ /api/content = 500 เพราะ DB ปิด (พิสูจน์ว่า route ติดตั้งแล้ว)

  ▌ ยังไม่ได้ทำ/ต้องทำต่อ (ดู 06):
  - เครื่อง serverdew นี้ MySQL80 ปิดอยู่ + ไม่มี Docker → รัน DB ไม่ได้ตอนนี้
    (start service ต้องสิทธิ์แอดมิน) ยังทดสอบ flow เต็ม (login→แก้→บันทึก→เห็นผล)
    ไม่ได้ — ต้องเปิด DB หรือทดสอบหลัง deploy บน HostAtom
  - รัน migration_support_admin.sql + create_support_admin.js บน DB
  - ยังไม่ได้ commit/push (รวมงาน Phase 3 รอบ 12 ที่ค้างด้วย)

▌ 2026-07-03 (รอบ 12 — เปิด Phase 3: ระบบแบบประกันจริง) — Claude Sonnet 5
  ★ พี่ดิว: กดที่การ์ด "สอบถามรายละเอียด" 6 ใบในหน้าแรก แล้วบอกว่าอยากให้
    "สร้างหน้าเพิ่มแล้วกดไปยังหน้าแนะรายละเอียดเพิ่มเติม"
  ★ ถามกลับว่าจะเอาแบบไหน (static หน้าเดียว / static แยกหน้า / DB Phase 3
    เต็มรูป) → พี่ดิวเลือก "รอระบบแบบประกันเต็มรูป (Phase 3)"
    → เริ่มทำ Phase 3.1 + 3.2 (ตามแผนใน 04_แผนการรื้อใหม่.txt) เลย

  ◉ ตัดสินใจเรื่องหมวดหมู่ (Q4 ใน 08_คำถามรอตอบ ที่ค้างมานาน):
    ใช้ 6 หมวดตามที่มีอยู่จริงในหน้าแรกตอนนี้ (health/life/savings/
    retirement/group/legacy) ไม่ใช่ 3 หมวดจาก PDF/dashboard design เก่า
    เพราะ 6 หมวดคือของจริงที่ deploy อยู่ในหน้าแรกแล้ว

  ◉ DB Schema (db/migration_insurance_plans.sql + เพิ่มใน schema.sql ด้วย):
  ✓ insurance_categories (id, slug, name, icon, sort_order) — seed 6 หมวด
  ✓ insurance_plans (id, user_id, category_id, title, summary,
     content_html, cover_image, is_active, view_count, timestamps)
     FK ผูกกับ users(id) แบบ ON DELETE CASCADE
  ★ ยังไม่ได้รันบน production DB จริง! ต้องรัน migration_insurance_plans.sql
    ผ่าน phpMyAdmin ก่อน deploy ฟีเจอร์นี้ถึงจะใช้งานได้จริง

  ◉ API (routes/plans.js — mount ที่ /api ใน index.js):
  ✓ GET /api/categories, GET /api/plans (filter category/user_id, public
     เห็นเฉพาะ is_active), GET /api/plans/mine (ของตัวเอง รวมที่ซ่อน),
     GET /api/plans/:id (public, นับ view_count), POST/PUT/DELETE /api/plans
     (requireAuth + เช็ค ownership ก่อนแก้/ลบเสมอ)
  ✓ Multer upload cover_image แยกโฟลเดอร์ public/uploads/plans/<user_id>/
  ✓ ป้องกัน orphaned upload file แต่แรก (เรียนจากบั๊กรอบ 7): มี
     cleanupUploadedFile(req) เรียกทุกจุดที่ validation fail หลัง multer
     เขียนไฟล์ลง disk ไปแล้ว

  ◉ Public pages:
  ✓ public/plans.html — list + filter tabs ตามหมวดหมู่ (ดึงจาก /api/categories)
  ✓ public/plan-detail.html — รายละเอียด 1 แบบ + ปุ่ม "สนใจแบบนี้" ลิงก์ไป
     /agent/:user_id#contact
  ★ ความปลอดภัย: content_html render เป็น plain text เท่านั้น (escapeHtml
     แล้วแปลง \n เป็น <br>) ไม่ใช้ innerHTML ดิบๆ เพราะเนื้อหามาจากตัวแทน
     กรอกเอง ถ้าไม่ escape จะเป็นช่องโหว่ stored XSS (ตัวแทนคนหนึ่งใส่
     payload แล้วไปโจมตี visitor คนอื่นที่มาดูหน้า /plans/:id ได้)
  ✓ แก้ public/index.html: การ์ดบริการ 6 ใบ ลิงก์จาก #contact →
     /plans?category=health|life|savings|retirement|group|legacy
  ✓ เพิ่ม "📋 แบบประกัน" ใน public/nav.js (โชว์ทุกหน้าที่ใช้ nav.js ร่วม)
  ✓ เพิ่ม routes /plans, /plans/:id ใน routes/pages.js

  ◉ Admin (public/dashboard.html):
  ✓ เปิดปุ่ม "จัดการแบบประกัน" ใน services section (เดิม disabled
     "🚧 รอเปิด" — ตอนนี้เปิดใช้งานจริงแล้ว)
  ✓ Modal data-modal="plans": list ของตัวเอง (edit/delete ต่อแถว) +
     ฟอร์ม add/edit (cover upload, หมวดหมู่, ชื่อ, คำโปรยสั้น, เนื้อหาเต็ม,
     toggle เปิด/ปิดแสดงผล) สลับ view ในโมดัลเดียว ไม่แยกหน้า /admin/plans
  ★ services section บน dashboard/index ยังเป็น static 6 การ์ดเหมือนเดิม
     (ตั้งใจไม่ผูกกับ DB ในรอบนี้ กันงานบวมเกินไป) ผู้เยี่ยมชมดูแบบประกัน
     จริงผ่าน /plans ที่แยกต่างหาก

  ◉ ทดสอบ:
  ✓ node --check ทุกไฟล์ (routes/plans.js, index.js, dashboard.html,
     plans.html, plan-detail.html) ผ่านหมด
  ✓ cross-check id/name/data-form ใน dashboard.html ครบ ไม่มี typo
  ✓ ทดสอบ routing จริงใน browser: /, /plans, /plans/1, /search, /agent/1
     → 200 ทั้งหมด / /dashboard, /profile → redirect /login ถูกต้อง
     (ทดสอบตอนไม่มี DB — ได้ผลลัพธ์ตามที่คาดคือ error แบบ graceful
     ไม่ crash ไม่ hang)
  ✓ ทดสอบ API: /api/categories, /api/plans, /api/plans/1 → 500 (คาดไว้
     เพราะไม่มี DB local), /api/plans/mine → 401 (requireAuth ทำงานถูก
     ก่อนแตะ DB ด้วยซ้ำ)
  ✗ ยังไม่ได้ทดสอบ end-to-end จริง (login → เปิดโมดัล → เพิ่ม/แก้/ลบ
     แบบประกันจริง → เห็นผลที่ /plans) เพราะเครื่องนี้ไม่มี MySQL local
     — รอ Docker Desktop เหมือนทุกรอบก่อนหน้า

  ▌ สิ่งที่ต้องทำก่อนใช้งานจริงบน production:
    1. รัน db/migration_insurance_plans.sql บน HostAtom phpMyAdmin
    2. Deploy โค้ด (push แล้ว Plesk auto-pull)
    3. Login แล้วลองกด "จัดการแบบประกัน" เพิ่มแบบประกันจริง 1 รายการ
       เช็คว่าขึ้นที่ /plans และ /plans/:id ถูกต้อง

▌ 2026-07-03 (รอบ 11 — ตรวจไฟล์/dependency ไม่ใช้) — Claude Sonnet 5
  ★ พี่ดิว: "ตรวจสอบความสมเหตุสมผลทั้งหมด ไฟล์ไหนไม่ใช้ลบออก
    ไปที่ AI_Memory เพิ่มสรุปให้อ่าน"

  ◉ วิธีตรวจ: grep ทุก require()/href/script-src จริงในโค้ด เทียบกับ
    ไฟล์/dependency ที่ declare ไว้ ไม่ได้เดา

  ◉ ลบแล้ว (ยืนยันไม่ได้ใช้ 100%):
  ✓ npm uninstall axios form-data mssql pg tedious — ไม่มี require()
    เลยสักจุดในโค้ดทั้งโปรเจกต์ (mssql/pg/tedious คือ driver DB อื่น
    ที่ไม่เกี่ยวกับโปรเจกต์นี้เลย น่าจะติดมาจาก scaffold ตอนแรก)
    ผลลัพธ์: ลบไป 102 packages (รวม transitive deps) node_modules เบาลง
    เยอะ ทดสอบรัน server แล้วปกติไม่กระทบอะไร

  ◉ เจอช่องโหว่ความปลอดภัยระหว่างตรวจ แก้ไปด้วย:
  ✓ multer 2.1.1 → 2.2.0 (npm audit fix) แก้ High severity DoS 2 จุด
    (nested field name flood, ไฟล์อัปโหลดที่ยกเลิกกลางทางไม่ถูกเคลียร์)
    npm audit หลังแก้ = 0 vulnerabilities

  ◉ เจอว่าไม่ได้ใช้ แต่ "ไม่ลบ" — ต้องรอพี่ดิวตัดสินใจเอง (เหตุผลอยู่ใน
    AI_Memory/09_ตรวจสอบไฟล์ไม่ใช้.txt):
  ？ public/src/S__12566769.jpg — ไม่มีหน้าไหน reference แล้ว (index.html
    เปลี่ยนไปใช้รูป stock จาก Unsplash แทน) แต่เป็นรูปจริงของลูกค้า
    ไม่ใช่ไฟล์ขยะ — รอพี่ดิวบอกว่าจะเก็บไว้ใช้ทีหลังมั้ย
  ？ db/alter_add_profile_fields.sql, alter_add_cover_images.sql — ไม่มี
    โค้ดรันอัตโนมัติ (ต้อง copy ไป phpMyAdmin เอง) แต่ยังไม่รู้ว่า
    production DB จริงมีคอลัมน์พวกนี้ครบหรือยัง — ไม่ลบเผื่อยังต้องใช้

  ◉ สร้างเอกสารสรุปให้พี่ดิวอ่านตรงๆ (ไม่ใช่แค่ AI):
  ✓ AI_Memory/09_ตรวจสอบไฟล์ไม่ใช้.txt — มีโครงสร้างไฟล์ทั้งหมดปัจจุบัน
    + รายการที่ลบแล้ว + รายการที่รอตัดสินใจ พร้อมเหตุผลแต่ละอย่าง
  ✓ เพิ่มไฟล์นี้เข้า index ใน 00_อ่านก่อน.txt แล้ว

▌ 2026-07-03 (รอบ 10 — ยุบ profile.html เข้า dashboard.html) — Claude Sonnet 5
  ★ พี่ดิว: "เอา profile ออก เอา dashboard มาทำเป็นโปรไฟล์
    profile อันเก่าเคยมีอะไรเอามารวมกันที่ dashboard"
    ตีความ: ลบ public/profile.html ทิ้ง ให้ public/dashboard.html ทำหน้าที่
    เป็นทั้งหน้าโปรไฟล์ตัวเอง (แก้ไขได้) และหน้าดูโปรไฟล์คนอื่น (read-only)

  ★ นี่คือการเปลี่ยนโครงสร้างใหญ่ — สรุปให้ AI รอบหน้าเข้าใจง่ายๆ:
    ก่อนหน้านี้มี 2 ไฟล์แยกกัน (profile.html = ดูโปรไฟล์ทั้งตัวเองและคนอื่น,
    dashboard.html = หน้าจัดการแบบ preview-landing) ตอนนี้เหลือไฟล์เดียว
    (dashboard.html) ที่สลับโหมดเองจาก URL path ที่เรียก:
      /profile, /dashboard → โหมดตัวเอง (ต้อง login, มี edit-btn ทุกจุด,
                              admin-bar, ปุ่ม logout, ปุ่ม "มุมมองผู้มุ่งหวัง")
      /agent/:id            → โหมดดูคนอื่น (public, ไม่ต้อง login,
                              ซ่อน admin-bar ทั้งหมด, ซ่อนปุ่มแก้ไขทุกจุด,
                              header label กลายเป็นลิงก์ "← กลับหน้าหลัก")
    ตรวจสอบโหมดด้วย regex `/^\/agent\/(\d+)/` ตรง path — ตัวแปร isViewingOther

  ◉ Routing (routes/pages.js):
  ✓ /profile, /dashboard, /agent/:id ทั้ง 3 ตอนนี้ serve public/dashboard.html
    (ไม่ต้องแก้ index.html/login.html/search.html เพราะ URL เดิมไม่เปลี่ยน
    แค่เปลี่ยนไฟล์ที่ตอบกลับ)
  ✓ ลบ public/profile.html ทิ้งแล้ว (เช็คแล้วไม่มีไฟล์อื่น reference)

  ◉ ย้ายฟีเจอร์จาก profile.html เดิม เข้า dashboard.html (ของที่ dashboard
    เดิมไม่มี แต่ profile.html เคยมี):
  ✓ Cover images slideshow (สูงสุด 6 รูป, fade 1.5s) — เพิ่มเป็นพื้นหลัง
    ของ section hero (#cover-slideshow, #cover-overlay ซ้อนใต้ hero-inner)
  ✓ Avatar upload ในโมดัลแก้โปรไฟล์ (ก่อนหน้านี้ dashboard แก้ avatar ไม่ได้เลย)
  ✓ Cover images upload/ลบ ในโมดัลแก้โปรไฟล์ (add/remove, preview grid, MAX 6)
  ✓ กล่องแสดงเลขใบอนุญาต (license_number, license_number_2) ใต้ตำแหน่งงาน
    ในส่วน "รู้จักที่ปรึกษาของคุณ" — ซ่อนถ้าไม่มีข้อมูล
  ✓ ฟิลด์ license_number, license_number_2, bio เพิ่มในโมดัลแก้ไขโปรไฟล์
    (bio ยังไม่แสดงบนหน้าเว็บ เก็บไว้ก่อนเหมือนที่ profile.html เดิมทำ)
  ✓ ปุ่ม "👁️ มุมมองผู้มุ่งหวัง" ใน admin-bar (โชว์เฉพาะโหมดตัวเอง หลังโหลด
    user.id) → เปิด /agent/{ownId} ในแท็บใหม่

  ◉ Public-view mode (isViewingOther) — เพิ่มใหม่ทั้งหมด:
  ✓ ซ่อน #admin-bar ทั้งก้อน (มี logout, ดูหน้าเว็บ, มุมมองผู้มุ่งหวัง)
  ✓ ซ่อนปุ่ม .edit-btn ทุกจุด (hero/profile/services/reviews/awards)
  ✓ header label "หน้าจัดการ" เปลี่ยนเป็นลิงก์ "← กลับหน้าหลัก"
  ✓ document.title เปลี่ยนเป็น "โปรไฟล์ตัวแทน — VP Kanniporn"
  ✓ fetch ข้อมูลจาก /api/users/:id แทน /api/auth/me
  ✓ ถ้าไม่พบ (404) แสดงข้อความ "ไม่พบตัวแทนรายนี้" แทนที่ <main> ทั้งหมด
    (ทดสอบจริงแล้วผ่าน — /agent/2 ตอนไม่มี DB local แสดงข้อความนี้ถูกต้อง)
  ✓ ไม่ใช้ localStorage hero-override ตอนดูคนอื่น (กันเห็นค่าที่ยังไม่ save DB)

  ◉ ทดสอบ:
  ✓ node --check ผ่าน (แยก script ออกมาเช็ค syntax)
  ✓ cross-check ทุก getElementById(...) ในสคริปต์ (31 จุด) ตรงกับ id="..."
    ในหน้า HTML จริงครบ ไม่มี id ขาด ไม่มี id ซ้ำ
  ✓ cross-check ทุก form.elements.X (16 จุด) ตรงกับ name="X" ใน input ครบ
  ✓ ทดสอบจริงใน browser: /agent/2 (ไม่มี DB) → admin-bar ซ่อน, edit-btn
    ซ่อนหมด, header กลายเป็น "← กลับหน้าหลัก", แสดง "ไม่พบตัวแทนรายนี้" ถูกต้อง
  ✓ ทดสอบจริงใน browser: /profile, /dashboard (ไม่ login) → redirect
    ไป /login ถูกต้องเหมือนเดิม (auth gate ยังทำงานปกติ)
  ✗ ยังไม่ได้ทดสอบโหมดตัวเอง (login แล้วเห็น edit-btn + avatar/cover upload
    จริง) เพราะเครื่องนี้ไม่มี MySQL local — รอ Docker Desktop เหมือนเดิม

  ◉ อัปเดต README.md: โครงสร้างโปรเจกต์ + ตาราง Endpoints ให้ตรงกับ
    ไฟล์จริงหลังยุบรวม (ลบ profile.html ออกจากลิสต์, เพิ่ม dashboard.html
    อธิบาย 2 โหมด, เพิ่ม /api/users, /api/users/:id, PUT /api/auth/profile
    ที่หายไปจากตารางเดิม)

  ▌ ยังไม่ push — ตอนนี้มีงานค้าง push สะสมหลายรอบ (6-10) รอพี่ดิวสั่ง

▌ 2026-07-03 (รอบ 9 — confirm popup ก่อนออกจากระบบ) — Claude Sonnet 5
  ★ พี่ดิว: "ตอนที่กดออกจากระบบ มี popup แจ้งยืนยันก่อนอีกครั้ง"
    ตีความ: พี่ดิวอยากให้มี popup ถามยืนยันก่อน logout ทุกที่ (กันกดพลาด)

  ◉ ตรวจพบว่าไม่สม่ำเสมอ — มีแค่ dashboard.html ที่ถามยืนยันอยู่แล้ว
    (`if (!confirm('ออกจากระบบ?')) return;`)
    ส่วน public/index.html (ปุ่ม cta-logout) และ public/profile.html
    (ปุ่ม logout-btn ตอน logout ตัวเอง ไม่ใช่ตอนกด "กลับหน้าหลัก"
    ของคนอื่น) กด logout ทันทีไม่มีถามอะไรเลย

  ◉ แก้ให้ตรงกันทั้ง 3 หน้า — เพิ่ม `if (!confirm('ออกจากระบบ?')) return;`
    ก่อนเรียก fetch('/api/auth/logout') ใน:
    ✓ public/index.html (บรรทัด ~973)
    ✓ public/profile.html (บรรทัด ~524 — เฉพาะ branch logout ตัวเอง
       ไม่กระทบปุ่ม "กลับหน้าหลัก" ตอนดูคนอื่น)
    (dashboard.html มีอยู่แล้วไม่ต้องแก้)

  ◉ ทดสอบ: preview_eval stub `window.confirm` แล้วจำลองคลิกปุ่ม cta-logout
    บน index.html → ยืนยันว่า confirm() ถูกเรียกด้วยข้อความ "ออกจากระบบ?"
    ก่อน fetch จริง (ผ่าน) — profile.html เทสแบบเดียวกันไม่ได้เพราะต้อง
    login จริงถึงจะเห็น logout-btn ในโหมด "ตัวเอง" (ไม่มี MySQL local
    ยืนยันแค่โค้ด pattern เหมือนกันกับ index.html ที่เทสผ่านแล้ว)

▌ 2026-07-03 (รอบ 8 — bug จริงที่ทำให้ save profile 500 บน production) — Claude Sonnet 5
  ★ พี่ดิวรายงาน: หน้า /agent/2 ไม่อัปเดตตามที่แก้ใน /dashboard
    console error จริงจาก production:
      PUT https://www.vpkanniporn.com/api/auth/profile → 500
      {error: "เกิดข้อผิดพลาด กรุณาลองใหม่"} (ข้อความจริงถูกซ่อนเพราะ NODE_ENV=production)

  ◉ หาสาเหตุจาก client-side (public/dashboard.html) เพราะข้อความ error จริง
    ถูกซ่อนไว้ (ต้องรอ log จาก Plesk ถ้าจะดูของจริง แต่เจอ bug ชัดเจนจากโค้ดก่อน)

  ★ Root cause เจอแล้ว — 100% ชัวร์ ไม่ต้องเดา:
    ฟังก์ชัน saveProfile() (dashboard.html บรรทัด ~937) เดิม:
      fd.append('first_name', currentUser.first_name || '');   // (1) เขียนก่อนเสมอ
      fd.append('last_name', currentUser.last_name || '');
      for (const [k, v] of Object.entries(updates)) fd.append(k, v); // (2) เขียนทับ

    ปุ่ม "บันทึกโปรไฟล์" (form submit บรรทัด ~989) ส่ง updates ที่มี
    first_name/last_name อยู่แล้ว → fd.append ถูกเรียกซ้ำ 2 ครั้งสำหรับ
    key เดียวกัน → FormData มี 2 ค่าใน field เดียวกัน
    → multer parse เป็น req.body.first_name = ["", "ชื่อใหม่"] (array แทน string)
    → pool.query('UPDATE users SET first_name = ?, ...', [arrayค่านี้, ...])
      mysql2 แปลง array parameter เป็น comma list (สำหรับ IN clause)
      แต่ context นี้เป็น SET column = ค่าเดียว → กลายเป็น SQL ผิด syntax
      → throw error → next(err) → 500 (โดน NODE_ENV=production ซ่อนข้อความ)

    ★ เกิดกับทุกครั้งที่กด "บันทึกโปรไฟล์" (มี first_name/last_name ใน updates)
      ไม่เกิดกับ "บันทึกผลงาน" (awards) เพราะ updates ไม่มี first_name/last_name ซ้ำ

  ◉ แก้แล้ว (dashboard.html):
  ✓ เปลี่ยนเป็น merge เป็น plain object ก่อน (`{...default, ...updates}`)
     แล้วค่อย fd.append ทีเดียวต่อ 1 key — ห้ามซ้ำ
  ✓ ทดสอบ logic การ merge ผ่าน preview_eval จำลอง FormData.getAll()
     ยืนยันว่าแต่ละ field เหลือค่าเดียวแล้ว (ไม่ใช่ array)
  ✗ ยังทดสอบ end-to-end ผ่าน login+save จริงไม่ได้ (ไม่มี MySQL local)

  ▌ สรุปสถานะ bug: นี่คือของแท้ ทำให้ "แก้โปรไฟล์ไม่เซฟ" ตามที่พี่ดิวเจอ
    ไม่ใช่เรื่อง session store (รอบ 6) หรือไฟล์ค้าง (รอบ 7) — คนละเรื่องกัน
    แต่ 3 เรื่องอยู่ใน commit เดียวกันที่ยังไม่ได้ push (35501c7 push ไปแล้ว
    เป็นแค่ session-store, ส่วนรอบ 7-8 ยังอยู่ใน working tree เครื่องนี้)

▌ 2026-07-03 (รอบ 7 — เช็ค API แก้ไขโปรไฟล์) — Claude Sonnet 5
  ★ พี่ดิว: "เช็ค api แก้ไขข้อมูล profile หน่อย"
  (เกิดขึ้นหลังพบว่า commit 35501c7 ของรอบ 6 ถูก push ขึ้น origin/main แล้ว
   ทำให้เว็บจริง crash — Passenger error page — สาเหตุคาดว่า Plesk
   npm install ไม่ทัน dependency ใหม่ ยังรอพี่ดิวเช็ค log ใน Plesk panel)

  ◉ ตรวจ PUT /api/auth/profile (routes/auth.js) พบบั๊ก 2 จุด — แก้แล้ว:

  1) ไฟล์ภาพปก (cover_images) ค้าง disk ถ้าอัปโหลดรวมกันเกิน 6 รูป
     เดิม: `[...validKept, ...newPaths].slice(0, 6)` ตัดของเกินทิ้งเฉย ๆ
     ไฟล์ใหม่ที่ multer เขียนลง disk ไปแล้ว (ก่อน handler รันด้วยซ้ำ)
     ไม่เคยถูกลบ เพราะ logic ลบไฟล์เดิมเช็คแค่ oldCovers ไม่เช็ค newPaths
     → กลายเป็นขยะสะสมใน public/uploads/covers/<id>/ ตลอดไป
     แก้: เก็บ `combined` ก่อน slice แล้วเทียบว่า path ไหนใน newPaths
     ที่หลุดจาก merged ก็ส่งเข้า filesToDelete ด้วย
     (หน้า UI ปกติ (profile.html MAX_COVERS=6) ป้องกันเคสนี้อยู่แล้ว
      แต่ API เองไม่ควร trust ฝั่ง client เพียงอย่างเดียว)

  2) ไฟล์ที่อัปโหลดใหม่ (avatar/cover) ค้าง disk ถ้า validation fail ทีหลัง
     multer เขียนไฟล์ลง disk ก่อน handler เริ่มทำงานเสมอ (เป็น middleware
     ก่อนหน้า) ถ้า validation fail ที่หลัง (เช่น awards ไม่ใช่ JSON array,
     cover_images_keep ไม่ใช่ JSON array, gender ไม่ถูกต้อง) โค้ดเดิม
     return error ทันทีโดยไม่ลบไฟล์ที่เพิ่งอัปโหลด → ค้างเป็นขยะเหมือนกัน
     แก้: เพิ่ม `cleanupUploadedFiles(req)` helper ลบไฟล์ทุกไฟล์ใน req.files
     เรียกก่อน return error ในทุกจุดที่ validation อาจ fail

  ◉ ทดสอบ:
  ✓ node --check routes/auth.js ผ่าน (syntax ok)
  ✓ npm run dev บูตปกติ ไม่มี regression
  ✗ ยังทดสอบ end-to-end จริงไม่ได้ (upload + validation fail จริง) เพราะ
     เครื่องนี้ยังไม่มี MySQL local (รอ Docker Desktop เหมือนเดิม)

  ◉ ยังไม่ได้ commit/push — รอพี่ดิวสั่ง (โดยเฉพาะตอนนี้เว็บจริงกำลัง
     debug ปัญหา Passenger crash จาก push รอบก่อนอยู่ ควรรอให้ตรงนั้นนิ่งก่อน)

▌ 2026-07-03 (รอบ 6 — session store persistent) — Claude Sonnet 5
  ★ พี่ดิว: "ต้องการให้จำการ login"

  ◉ วินิจฉัยสาเหตุ:
    ก่อนหน้านี้ session cookie ตั้ง maxAge 30 วันไว้แล้ว (ทำตั้งแต่รอบก่อนๆ)
    แต่ session middleware ไม่ได้ตั้งค่า `store` → ใช้ default MemoryStore
    ของ express-session ซึ่งเก็บ session ไว้ใน RAM ของ process เดียว
    → ทุกครั้งที่ Node process restart (deploy ใหม่, crash, หรือแม้แต่
      `node --watch` ตอน dev ที่ restart เองเวลาไฟล์เปลี่ยน) session หายหมด
      ผู้ใช้ต้อง login ใหม่ทั้งที่ cookie ยังไม่หมดอายุ ← นี่คือสาเหตุที่ "ไม่จำ login"

  ◉ แก้ไข (index.js):
  ✓ เพิ่ม dependency `express-mysql-session` (^3.0.3) — session store ที่เก็บ
     ลง MySQL table `sessions` แทน RAM ใช้ pool เดิมจาก db/pool.js (ไม่เปิด
     connection ซ้ำ) → session อยู่รอดข้าม process restart ได้จริง
  ✓ ตั้ง expiration: 30 วัน ตรงกับ cookie.maxAge เดิม
  ✓ createDatabaseTable: true → table `sessions` จะถูกสร้างอัตโนมัติตอน
     boot ครั้งแรก (ไม่ต้องรัน SQL เพิ่มเอง ไม่ต้องแก้ schema.sql)
  ✓ เพิ่ม `.on('error', ...)` กัน process crash ถ้า DB ต่อไม่ได้ตอน boot
     (EventEmitter 'error' ที่ไม่มี listener จะทำให้ Node throw ทั้ง process)
  ✓ graceful shutdown เพิ่ม `sessionStore.close()` ก่อน `pool.end()`
     (ปิด timer เคลียร์ session หมดอายุ โดยไม่ปิด pool เพราะใช้ร่วมกับ query อื่น
     — เช็ค source code ของ lib แล้วว่า endConnectionOnClose=false เมื่อ
     ส่ง pool เข้าไปเอง ปลอดภัย ไม่ปิด pool ซ้ำ)

  ◉ ทดสอบ (เครื่องนี้ยังไม่มี Docker/MySQL local — ทดสอบเท่าที่ทำได้):
  ✓ npm run dev บูตสำเร็จ ไม่ crash แม้ DB ต่อไม่ได้ (error listener ทำงาน)
  ✓ GET / และ GET /api/auth/me ตอบ 200 ปกติ (ไม่ต้องอ่าน session store
     ถ้ายังไม่มี cookie เดิม)
  ✓ POST /api/auth/login ตอบ 500 เร็ว (18ms) ไม่ hang — fail fast ตามคาด
     เพราะ query DB ไม่ได้ (พฤติกรรมเดิมก่อนแก้ก็เป็นแบบนี้ ไม่ได้แย่ลง)
  ✗ ยังไม่ได้ทดสอบ "login แล้ว restart server แล้วยัง login ค้างอยู่มั้ย"
     จริงๆ เพราะต้องมี MySQL ก่อน (รอ Docker Desktop)

  ▌ ทำต่อรอบหน้า (หลังพี่ดิวลง Docker + เปิด docker compose up -d):
    1. Register/login ทดสอบให้เห็น table `sessions` ถูกสร้างอัตโนมัติใน DB
    2. Restart dev server (kill + npm run dev ใหม่) แล้วเช็คว่า refresh
       browser แล้วยัง login อยู่ (ไม่ redirect ไป /login) — คือการยืนยันจริง
       ว่า "จำ login" ทำงาน
    3. Deploy ขึ้น HostAtom แล้วเช็คว่า table sessions ถูกสร้างบน production DB
       ด้วย (createDatabaseTable: true จะสร้างให้อัตโนมัติเหมือนกัน)

▌ 2026-07-03 (รอบ 5 — session ใหม่, เครื่องใหม่) — Claude Sonnet 5
  ★ พี่ดิวเปิดงานนี้เป็น "งานใหม่" บนเครื่อง user "serverdew"
    (โฟลเดอร์ FWD/ ทั้งหมดถูกย้าย/คัดลอกมาจากเครื่องเดิม user "dew")
    โจทย์ที่พี่ดิวบอก: "ต้องการเว็บที่ใช้งานได้ทั้งมือถือและเดสก์ท็อป"
    + "สร้างเอกสารไว้ให้ AI ไว้ให้คนถัดไปอ่านด้วย"

  ◉ ตรวจสอบสถานะก่อนทำอะไร (ไม่ได้แก้โค้ด แค่ยืนยัน):
  ✓ git log/status: HEAD ตรงกับ origin/main, working tree clean
     commit ล่าสุด 7ff6065 (2026-06-20 12:51) — ตรงกับที่บันทึกไว้ใน
     05_ทำแล้ว.txt รอบ 1-4 ครบถ้วน ไม่มีงานที่ทำไปแล้วแต่ลืมจด
  ✓ เช็ค public/index.html: มี viewport meta + clamp() + Tailwind
     breakpoint (sm:/md:/lg:) 14 จุด → responsive มือถือ/เดสก์ท็อป
     "ทำแล้ว" อยู่ก่อนหน้านี้ (ตั้งแต่รอบ 1) ไม่ต้องทำใหม่
  ✓ เช็ค index.js: static file serve ไม่ผูกกับ DB (lazy pool)
     → หน้า / และ /login เปิดได้แม้ยังไม่ต่อ MySQL
     (login/register/profile ต้องมี DB ทำงานถึงจะทดสอบได้จริง)

  ◉ พบว่าเครื่องนี้ยังไม่พร้อม dev — พี่ดิวเลือกให้ setup ต่อเลย:
  ✓ npm install — เสร็จ (200 packages, 2 high severity audit warnings —
     ยังไม่ได้ไล่แก้ เพราะเป็น dep เดิมจากโปรเจกต์ ไม่ใช่ของใหม่)
  ✓ สร้าง .env จาก .env.example — ใช้ค่า local Docker (ตรงกับ docker-compose.yml):
     DB_HOST=localhost DB_USER=vpkann_database DB_PASSWORD=devpass DB_NAME=vpkann_database
     SESSION_SECRET สุ่มใหม่ด้วย crypto.randomBytes(32)
     NODE_ENV=development (ของจริงบน HostAtom ต้องเปลี่ยนเป็น production ผ่าน Plesk env var
     — .env นี้อยู่บนเครื่อง local เท่านั้น ไม่ใช่ค่า production)
  ✓ สร้าง .claude/launch.json (สำหรับ preview tool ของ Claude Code เท่านั้น
     ไม่กระทบ deploy จริง) — ใช้ `npm --prefix <path> run dev`
  ✓ รัน dev server สำเร็จที่ port 3000 (node --watch index.js)
  ✓ ทดสอบ responsive จริงผ่าน browser preview:
     - Desktop viewport: layout เต็มความกว้าง, nav/hero/service grid ปกติ
     - Mobile viewport (375x812): text/ปุ่ม/card ปรับขนาดเป็น 1 คอลัมน์ อ่านง่าย
       ยืนยัน mobile+desktop responsive ใช้ได้จริง (ไม่ใช่แค่มี CSS class เฉยๆ)
  ✗ Docker Desktop ยังไม่ได้ลงบนเครื่องนี้ → MySQL local ยังไม่ขึ้น
     → /api/users/1 ตอบ 500 (คาดไว้แล้ว, มี fallback static ที่หน้าเว็บ ไม่พัง)
     → login/register/profile ยังทดสอบไม่ได้จนกว่าจะมี DB

  ◉ อัปเดตเอกสาร (งานหลักของรอบนี้):
  ✓ 07_ข้อมูลสำคัญ.txt — แก้ path ทุกจุดจาก C:\Users\dew\ → C:\Users\serverdew\
     + เพิ่มหัวข้อ "เครื่องนี้ยังไม่เคย setup dev env" กันงงตอนรันไม่ขึ้น
  ✓ 05_ทำแล้ว.txt (ไฟล์นี้) — เพิ่ม checkpoint รอบ 5

  ▌ ยังไม่ได้ถามพี่ดิว (รอตอบก่อนเริ่มโค้ดต่อ):
    - "งานใหม่" หมายถึงต่อยอด Phase 2/3 เดิม (ดู 06_ยังไม่ทำ.txt) หรือมีโจทย์ใหม่อื่น?
    - ลง Docker Desktop บนเครื่องนี้เมื่อไหร่ดี? (ต้องลงเองที่เครื่อง — AI สั่งลงให้ไม่ได้)
      หลังลงเสร็จ บอก AI รอบหน้า จะรัน `docker compose up -d` + import schema ให้

▌ 2026-06-19 (รอบ 4) — Claude Opus 4.7
  ★ พี่ดิว: "แก้หน้า Profile ให้เหมือน Home — มีเมนู 4 อัน + แก้ไขแบบเดิม"
    ตีความ: หน้า Dashboard (ตัวแทน login) ให้เป็น preview ของ landing
            + nav 4 อัน + ปุ่มแก้แต่ละ section

  ◉ รื้อ public/dashboard.html ใหม่ทั้งไฟล์
  ✓ Layout เหมือน landing เป๊ะ: hero / services 6 ใบ / profile / reviews / timeline
  ✓ Admin top bar สีเข้ม สติกกี้: avatar + ชื่อ + "ดูหน้าเว็บ" + ออกจากระบบ
  ✓ Section nav (sticky) 4 อัน:
      💼 วางแผนการเงิน → #about-agent
      🏥 ประกันสุขภาพ → #services
      💰 ออมเงินและมรดก → #services
      🏆 ผลงานและรีวิว → #reviews
  ✓ ปุ่มแก้ (✏️ แก้ไข) มุมขวาบนของแต่ละ section ที่แก้ได้
  ✓ Modal popup แก้ 3 section (เปิดใช้งานได้จริง):
      1. Hero — แก้ heading/tagline/sub-tagline (เก็บใน localStorage รอ DB field Phase 3)
      2. Profile — first/last name + position + company + quote
                  + phone + line_id + facebook + instagram (PUT /api/auth/profile)
      3. Awards — textarea 1 บรรทัด/รางวัล → save JSON array
  ✓ Section ที่ยังไม่เปิด (services, reviews) มีปุ่ม "🚧 รอเปิด" disabled พร้อม tooltip
  ✓ Toast แจ้งผลบันทึก (เขียวสำเร็จ/แดงล้ม) + ปิดอัตโนมัติ 3 วินาที
  ✓ Loading screen ตอน auth check (ถ้าไม่ login → redirect /login)

  ◉ ยกเลิก (แทนที่ของรอบ 2):
  ✗ Dashboard sidebar 6 อัน — แทนด้วย section nav 4 อันบนสุด

  ◉ Smoke test ผ่าน:
     GET /dashboard → 302 → /login
     dashboard.html serve ครบ nav 4 อัน

▌ 2026-06-19 (รอบ 3) — Claude Opus 4.7
  ★ พี่ดิวบอก: "เหมือนเรียก index.html ผิดไฟล์ — แก้และลบที่ไม่ใช้"
    → Launch preview เปิด root/index.html (เก่า) แทน public/index.html (ใหม่)

  ◉ Cleanup:
  ✓ ย้าย root/index.html (23KB, ดีไซน์ reference) → AI_Memory/reference_design_landing.html
     เก็บไว้อ้างอิงสีและโครงสร้าง ไม่ลบทิ้ง

  ✓ ลบ public/packages.html (placeholder ไม่ตรง requirement)
     — ลูกค้าต้องการ /plans dynamic (Phase 3) ไม่ใช่ static placeholder

  ✓ ลบ route /packages ใน routes/pages.js

  ✓ ลบ link /packages ใน public/nav.js (เหลือ หน้าหลัก + ค้นหาตัวแทน)

  ✓ แก้ public/index.html service cards 6 ใบ:
     link "/packages?category=..." → "#contact" (พา visitor ไป CTA ติดต่อ)

  ◉ UI improvements (สืบเนื่องจาก feedback "ไม่เห็น login/register"):
  ✓ public/index.html ปุ่ม header
     เก่า: วงกลม "→" icon เล็กๆ (เห็นยาก)
     ใหม่: ปุ่มข้อความ "✍️ สมัครสมาชิก" + "🔑 เข้าสู่ระบบ"
     หลัง login: "⚙ หน้าจัดการ" + "↩ ออกจากระบบ"
     Mobile (<640px): ซ่อนปุ่มสมัครสมาชิก เหลือแค่เข้าระบบ

  ✓ public/login.html: รับ window.location.hash === '#register'
     → เปิด tab สมัครสมาชิกอัตโนมัติเวลามาจาก landing

  ◉ Smoke test ผ่าน:
     GET /         → HTTP 200 (landing ใหม่)
     GET /packages → HTTP 404 (ลบสำเร็จ)
     GET /nav.js   → ไม่มี packages link
     GET /dashboard → 302 → /login (auth gate)

▌ 2026-06-19 (รอบ 2) — Claude Opus 4.7
  ★ พี่ดิวยืนยัน: ทดสอบด้วย Docker MySQL local, nav 6 อันอยู่ใน dashboard ตัวแทนเท่านั้น,
    "ผลงาน" = รางวัล/Timeline ความสำเร็จ

  ◉ ค้นพบที่ต้องแก้:
  ★ index.js (Express entry) หายไปจาก disk + ไม่อยู่ใน git → เขียนใหม่
  ★ เครื่องพี่ดิวไม่มี MySQL/XAMPP → ใช้ Docker

  ◉ Code:
  ✓ index.js — Express server เต็มรูปแบบ
     - express-session cookie 'fwd.sid' 30 วัน
     - mount /api/auth, /api/users, /
     - /health, /api/health/db
     - SPA fallback 404 → index.html
     - graceful shutdown (SIGINT/SIGTERM ปิด MySQL pool)
     - smoke test ผ่าน: /, /login, /search, /api/auth/me ทำงานปกติ

  ✓ docker-compose.yml — MySQL 8.0 + phpMyAdmin
     - port 3306 (MySQL) + 8080 (phpmyadmin GUI)
     - utf8mb4_unicode_ci, TZ=Asia/Bangkok
     - mount db/schema.sql ใน /docker-entrypoint-initdb.d/ → auto-init
     - volume fwd-mysql-data (persistent)
     - DB user: vpkann_database / pass: devpass (local)

  ✓ .env — เปลี่ยน DB_PASSWORD เป็น 'devpass' (local Docker)
     - production .env บน HostAtom ต้องใช้ pass จริง

  ✓ public/index.html — landing nav แบบ visitor-friendly
     - nav header เหลือ 2 อัน: หน้าแรก / ค้นหาตัวแทน
     - ปุ่มมุมขวาบน: ลูกศร → /login (default)
     - JS เช็ค /api/auth/me ตอน load: ถ้า login → ปุ่มเปลี่ยนเป็น ⚙ /dashboard

  ✓ public/dashboard.html — หน้าจัดการสำหรับตัวแทน (ใหม่)
     - Top header: logo + ชื่อตัวแทน + avatar + logout
     - Banner "คุณกำลังอยู่ในโหมดจัดการข้อมูล"
     - Sidebar 6 อัน:
       1. 💼 วางแผนการเงิน — placeholder
       2. 🏥 ประกันสุขภาพ — placeholder (รอ DB Phase 3)
       3. 💰 ออมเงินและมรดก — placeholder (รอ DB Phase 3)
       4. 🏆 ผลงานและรีวิว — ★ ใช้งานได้จริง ★
            - textarea 1 บรรทัด = 1 รางวัล
            - live preview เป็น tag pills
            - บันทึก → PUT /api/auth/profile (awards JSON)
       5. 🤝 ร่วมงานกับเรา — placeholder
       6. 📞 ติดต่อเรา — ★ ใช้งานได้จริง ★
            - แก้ phone / line_id / facebook_url / instagram_url
            - email read-only (เปลี่ยนที่หน้าโปรไฟล์)
     - Tab switching ด้วย URL hash
     - Auth check ตอน load: ถ้าไม่ login → redirect /login

  ✓ routes/pages.js — เพิ่ม route /dashboard (requireAuth → redirect /login)

▌ พี่ดิวต้องทำ (1 step):
  ◇ ลง Docker Desktop https://www.docker.com/products/docker-desktop/
    หลังลงเสร็จเปิด Docker Desktop แล้วบอก AI ต่อรอบหน้า
    AI จะรัน docker compose up -d ให้ → ทดสอบ register + login ได้จริง

▌ 2026-06-19 (รอบ 1) — Claude Opus 4.7
  ✓ สำรวจ codebase + DB schema เรียบร้อย
  ✓ อ่าน PDF + 4 รูปภาพในโฟลเดอร์ "สิ่งที่ต้องทำ/"
  ✓ ตีความเครื่องหมายแดงของลูกค้าได้ครบ 7 หน้า PDF
  ✓ สร้าง AI_Memory/ + เขียนเอกสารส่งต่อ 9 ไฟล์
  ✓ พี่ดิวยืนยัน: Step-by-step UI ก่อน, กวิสรา showcase, public landing first

  ◉ Code changes:
  ✓ public/index.html — รื้อใหม่เป็น marketing landing (Phase 1.1)
     - ดีไซน์ตาม S__13730279.jpg เป๊ะ: hero/services 6 ใบ/why-us 4 stats/
       profile + popular plans/3 testimonials/timeline 2022-25/career/CTA
     - สีจาก CSS variables FWD (--primary-orange #FF6B00)
     - มี nav.js placeholder + sticky LINE CTA ขวาล่าง
     - ปุ่ม login icon มุมขวาบน (→ /login)
     - ปุ่ม "ดูโปรไฟล์ฉบับเต็ม" → /agent/1
     - JS fetch /api/users/1 มา override ข้อมูลกวิสรา (มี fallback static)
     - Responsive: clamp() ทุก font + breakpoint 640px/1024px
     - Service cards ลิงก์ไป /packages?category=health|life|savings|...

  ✓ public/profile.html — ลบ awards + bio sections + เพิ่ม view-as-prospect (Phase 1.2)
     - ลบ section "แนะนำตัว" (bio block) — ตาม PDF หน้า 2 ที่ลูกค้าวง X
     - ลบ section "เกียรติประวัติและผลงาน" — ตาม PDF หน้า 2 ที่ลูกค้าวง X
     - เพิ่มปุ่ม "👁️ ดูในมุมมองผู้มุ่งหวัง" ข้างปุ่ม "แก้ไขโปรไฟล์"
       (โชว์เฉพาะตอนดูโปรไฟล์ตัวเอง, คลิก → /agent/{own-id})
     - เก็บ edit modal เดิม (bio + awards ยังบันทึก DB ได้ ใช้ในอนาคต)
     - เก็บ hero/CTA row/profile card/license box เดิม (ตรงตาม PDF)
     - ลบโค้ด JS render bio + awards (element ถูกลบ)

▌ ยังไม่ทำ:
  - Admin dashboard (S__13730278.jpg) — ขั้นต่อไป
  - Plans management (PDF หน้า 4-6)
  - Edit profile ใหม่ (PDF หน้า 7) — เพิ่ม Threads, Line OA, ลบสาขา
  - DB tables ใหม่ (insurance_plans, contact_submissions)
  - ทดสอบในเบราว์เซอร์จริง (พี่ดิวเปิดดู preview ได้)

════════════════════════════════════════════════════════════════
  (เพิ่มรายการใหม่ด้านบน — ลำดับใหม่สุดอยู่บนสุด)
════════════════════════════════════════════════════════════════
