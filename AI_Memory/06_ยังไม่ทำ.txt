════════════════════════════════════════════════════════════════
  Checkpoint: TODO List
  เขียนใหม่ทั้งไฟล์ 2026-07-16 โดย Claude Opus 4.8
  (ของเดิมเป็น Phase 1-4 ตั้งแต่ มิ.ย. ซึ่งเลิกใช้ไปแล้วตั้งแต่เปลี่ยนทิศรอบ 14-16)

  ★ อ่าน HANDOFF_2026-07-16.txt ก่อน — มี "5 เรื่องที่ต้องรู้ ไม่งั้นพลาดแน่นอน"
════════════════════════════════════════════════════════════════

▌ เก็บงานระบบบลอค (เล็กๆ ทำได้เลย)

  [ ] db/repair_blocks.sql — ตัด SELECT จาก information_schema ท้ายไฟล์ออก
      เพราะ HostAtom ให้ #1044 Access denied → ใช้กับ production ไม่ได้
      (ใช้ SHOW COLUMNS / SHOW TABLES แทน หรือตัดส่วนตรวจผลทิ้งเลย)
  [ ] db/diagnose_blocks.sql — ปัญหาเดียวกัน (ทั้งไฟล์เป็น information_schema)
      → แก้ให้ใช้ SHOW หรือลบไฟล์ทิ้ง
  [ ] db/migration_blocks.sql — ไฟล์เก่าที่พังแล้ว พิจารณาลบทิ้ง
      (ตัวที่ใช้จริงคือ insert_blocks_only.sql)

▌ ของที่พี่ดิวยังไม่ได้กรอก/อัป บนเว็บจริง (ไม่ใช่บั๊ก — แค่ยังไม่ได้ทำ)

  [ ] hero: อัปรูป 18 ช่อง (6 ภาพ × 3 อุปกรณ์) — ตอนนี้มีแต่ข้อความ ยังไม่มีภาพเลย
  [ ] agents: เลือกตัวแทน 3-5 คน (data.agent_ids ยังว่าง → ส่วนนี้ยังไม่โชว์การ์ด)
      กดปุ่ม "👥 เลือกตัวแทนที่จะโชว์" บนหน้าเว็บตอนล็อกอินเป็น admin
      ★ 19 ก.ค. เปลี่ยนเป็นต้อง "พิมพ์ชื่อค้นหา" แล้ว — เปิดมาจะยังไม่เห็นรายชื่อใคร
        (ไม่ใช่บั๊ก พี่ดิวสั่งไม่ให้แสดงทั้งหมด)
      ★ 20 ก.ค. เพิ่มปุ่ม "👥 แสดงทั้งหมด" ข้างช่องค้นหาแล้ว — กดถ้าอยากเห็นทุกคน
      ★ 20 ก.ค. (รอบ 2) เอาเพดาน 5 คนออกแล้ว เลือกกี่คนก็ได้ — มีปุ่ม "✅ ติ๊กทุกคน"
        และการ์ดเปลี่ยนเป็นแถวเลื่อนซ้าย-ขวา เลยไม่ล้นหน้าแม้เลือกเป็นร้อยคน
  [ ] cta: ตั้ง appointment_agent_id (ยังว่าง → ปุ่ม "นัดหมายปรึกษา" ยังกดไม่ได้)
      เลือกจาก dropdown ในบลอค cta บนหน้าเว็บ
  [ ] recruit: ใส่ภาพ (พี่ดิวสั่งว่าบลอคนี้ใส่ภาพอย่างเดียว) — ยังไม่มีภาพ

▌ ฟีเจอร์ที่ API พร้อมแล้ว แต่ยังไม่มีปุ่มบนหน้าเว็บ

  [ ] บลอค type='text' (ข้อความอิสระ) — BLOCK_TYPES รองรับแล้ว ขาด UI ปุ่ม "เพิ่มบลอค"
  [ ] บลอค type='image' (รูปอิสระ) — เหมือนกัน
  [ ] ลากสลับลำดับบลอค — ตอนนี้ใช้ปุ่ม ▲▼ (ใช้ได้ปกติ) ถ้าอยากได้ลากต้องทำเพิ่ม

▌ หน้าโปรไฟล์ตัวแทน (dashboard.html) — ยังเป็นระบบเก่า

  [ ] ยังใช้ users columns (~35 คอลัมน์) + ปุ่ม ✏️ → โมดัล — ยังไม่ได้รื้อเป็นบลอค
      ★ ถ้าจะรื้อ: ใช้ page_blocks ต่อได้เลย ใส่ user_id = id ตัวแทน แทน NULL
        (ออกแบบเผื่อไว้แล้ว ไม่ต้องทำ schema ใหม่)
      ★ ถามพี่ดิวก่อนว่าจะรื้อไหม — 16 ก.ค. พี่บอกว่า "ไม่ต้องแล้ว ทำใหญ่โตเดี๋ยวเหนื่อย"
        อย่าเริ่มเองโดยไม่ถาม
  [ ] ถ้ารื้อแล้ว ค่อยลบคอลัมน์เนื้อหาออกจาก users (hero_*, cover_images, awards,
      quote, bio, recruit_visible, consult_order, social urls ฯลฯ) — สำรอง DB ก่อน

▌ เอกสาร / ความสะอาด

  [ ] README.md ล้าสมัยหนัก — ยังเขียนว่า / = หน้าค้นหา, ไม่มีเรื่องระบบบลอคเลย
      และยังบอกว่า DB เป็น MySQL (ผิด — เป็น MariaDB)
  [ ] AI_Memory/02_สถานะปัจจุบัน.txt ล้าสมัย (เขียนตั้งแต่ 3 ก.ค.)
  [ ] AI_Memory/01,03,04 เขียนตั้งแต่ มิ.ย. ก่อนเปลี่ยนทิศ — พิจารณาเก็บเป็นประวัติ
  [ ] docker-compose.yml เขียน mysql:8.0 ซึ่งไม่ตรงกับ production (MariaDB)
      → แก้เป็น mariadb หรือใส่คอมเมนต์เตือนไว้ ไม่งั้น AI ตัวถัดไปจะหลงเหมือนผม
  [ ] site_settings + home_featured_agent_id ค้างใน DB ไม่มีโค้ดใช้แล้ว — ลบได้
  [ ] /search ซ้ำกับ /home — พิจารณาลบ
  [ ] /plans, /plans/:id (Phase 3 แบบประกัน) อาจไม่ใช้แล้วในทิศทางใหม่ — ถามพี่ดิว
  [ ] รูป S__12566769.jpg ยัง hardcode อยู่ใน home.html (hero ฝั่งขวา)
      → ถ้าจะให้แอดมินเปลี่ยนเองต้องทำเป็นบลอค/ช่องอัป

▌ ความปลอดภัย (ทำแล้วบางส่วน)

  ✓ เขียนบลอคได้เฉพาะ role 'admin' (403 ทุกเส้นถ้าไม่ใช่) — เทสต์แล้ว
  ✓ XSS: sanitize content + escape ตอนแสดง — เทสต์บน DOM จริงแล้ว
  ✓ ระบบ impersonate (สวมสิทธิ์เป็น user คนอื่น) ถูกลบทิ้งแล้ว
  [ ] เปลี่ยนรหัส SupperAdmin จาก 1234 (ยังไม่ได้เปลี่ยน — ค้างมาตั้งแต่รอบ 14)

════════════════════════════════════════════════════════════════
